/**
	Третья программа
	@алгоритм переворачивания строки
	P.S. алгоритм может понадобиться, допустим, для определения слов, которые читаются как слева направа, так и наоборот(палиндромы)
	Пример: "шалаш"
**/

string str = "а роза упала на лапу Азора"
main
{
	int size = 0
	
	/**
		подсчёт количества символов в строке.
		Последний симлов '~'
	**/
	
	int i=0
	while(elem(str,i)!='~')				// можно было сразу воспользоваться функцией length
	{
		size = size+1
	}
	
	string buf
	int m
	char a
	/**
		переворачивание строки
	**/
	int j=size-1
	for(; j>=0; j--)
	{
		a = elem(str,j)
		buf = buf+ToString(a)
		m = m+1
	}
}