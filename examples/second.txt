/**
	@второй пример
	типы данных, имеющиес€ в €зыке:
		char
		string
		int
		void
**/

/**
	объ€вление глобальных переменных
**/
string str = "Hello world"
string s
char ch = 'a'
char b
int kol = 10

/**
	глобальна€ функци€
**/
int func(int j)
{
	int k = i+15
	return k
}

/**
	функци€ main
**/

main
{
	// присваивание значени€ глобальным переменным
	b = 'b'
	s = "Hello world"
	
	// чтение файла в строку
	string text = ReadFile("D:\921701\Yakimchik\test.txt")
	
	text = text + "good"
	
	//запись в файл текста
	WriteToFile("D:\921701\Yakimchik\test.txt",text)
	
	text = "Hello"+"Java"
	string s = "Hello"+"Antlr"
	
	//условные операторы
	if(s==str)
	{
		print("Good")
	}
	else
	{
		if(s=="Hello")
		{
			int i=14
			print(i)
			break								// прерывание и выход к следующей операции, т.е. print("Bad") не будет выполнено
		}
		print("Bad")
	}
	
	/**
		цикл for
		@ elem(string str, int i) - функци€, преобразовывабща€ i-ы элемент в char
		  length(string str) - функци€, определ€юща€ длину строки
	**/
	int k=0
	for(int j=0; j<length(text); j++)
	{
		if(elem(text,j)=='l')			//подсчЄт сколько букв в в строке "Hello world"
		{
			k = k+1
		}
	}
	print(k)
	
	/**
		цикл while
		~ - элемент, обозначающий конец строки
	**/
	while(elem(str,i)!='~')
	{
		char v = elem(str,i)
		WriteToFile("D:\921701\Yakimchik\test2.txt",v)
		i=i+1
	}
	
	int sum = k+15+kol+func(k)			// работа с int и вызов глобальной функции, возращающей тип int
}