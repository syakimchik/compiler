/**
	@второй пример
	типы данных, имеющиеся в языке:
		char
		string
		int
		void
**/

/**
	объявление глобальных переменных
**/
string str;
string s;
char ch;
char b;
int kol;

/**
	глобальная функция
**/
int func(int j)
{
	int k;
	k = j+15;
	return k;
}

/**
	функция main
**/

main
{
	// присваивание значения глобальным переменным
	b = 'b';
	s = "Hello world";
	
	// чтение строки с консоли
	string text;
	read(text);
	
	// чтение числа с консоли
	int numb;
	read(numb);
	
	text = text + "good";
	// вывод на экран
	write(text);
	
	text = "Hello"+"Java";
	
	//условные операторы
	if(equals(str,s))
	{
		write("Good");
	}
	else
	{
		if(equals(str,"Hello"))
		{
			int i;
			i =14;
			write(i);
		}
		
		if(!equals(s,"world") && !equals(s,"Hello"))	// && - обозначает "и". Знак "||" - обазначает или
		{
			write("no");
		}
	}
	
	if('a'<'b')
	{
		write("Good");
	}
	else{
		write("bad");
	}
	
	string hg;
	hg = "strABC";
	if(hg=="strABC")
	{
		write("Good");
	}
	
	string subStr;
	subStr = subString(hg, 2);			// результат - подстрока ABC. поиск осуществляется от заданного элемента до указанного.
	subStr = subString(hg, 2, 4);			// результат - подстрока AB. указывается индекс начала и конца.
	
	subStr = replace(hg,"A");			//заменяет в строке все A
	subStr = replaceFirst(hg, "A");		//заменяет в строке первый найденный элемент
	
	/**
		цикл for
		@ elem(string str, int i) - функция, преобразовывабщая i-ы элемент в char
		  length(string str) - функция, определяющая длину строки
	**/
	int k;
	k=0;
	int j;
	char ch;
	ch = elem(text,j)
	for(j=0; j<15; j=j+1)
	{
		if(contains(text, "l"))			//подсчёт сколько букв в в строке "Hello world"
		{
			k = k+1;
		}
	}
	write(k);
	
	/**
		цикл while
		~ - элемент, обозначающий конец строки
	**/
	while(elem(str,i)!='\n')
	{
		char v;
		v = elem(str,i);
		string v_str;
		v_str = ToString(v);
		write(v_str);
		i=i+1;
	}
	
	int sum;
	int sum1;
	sum= k+15;
	sum1 = kol+func(k);
	sum = sum-sum1;
}