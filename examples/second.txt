/**
	@второй пример
	типы данных, имеющиеся в языке:
		char
		string
		int
		void
**/

/**
	объявление глобальных переменных
**/
string str = "Hello world"
string s
char ch = 'a'
char b
int kol = 10

/**
	глобальная функция
**/
int func(int j)
{
	int k = j+15
	return k
}

/**
	функция main
**/

main
{
	// присваивание значения глобальным переменным
	b = 'b'
	s = "Hello world"
	
	/**
		*name: чтение файла в строку
		*ReadFile(string arg)
		*arg - path to file
	**/
	string path = "D:\921701\Yakimchik\test.txt"
	string text = ReadFile(path)
	
	text = text + "good"
	
	/**
		*name: запись в файл текста
		*WriteToFile(string first_arg, string secknd_arg)
		*first_arg: path to file
		*second_arg: what information write to file
	**/
	WriteToFile("D:\921701\Yakimchik\test.txt",text)
	
	text = "Hello"+"Java"
	string s1 = "Hello"+"Antlr"
	
	//условные операторы
	if(s==str)
	{
		print("Good")
	}
	else
	{
		if(s=="Hello")
		{
			int i=14
			print(i)
			break								// прерывание и выход к следующей операции, т.е. print("Bad") не будет выполнено
		}
		print("Bad")
	}
	
	/**
		цикл for
		@ elem(string str, int i) - функция, преобразовывабщая i-ы элемент в char
		  length(string str) - функция, определяющая длину строки
	**/
	int k=0
	for(int j=0; j<length(text); j++)
	{
		if(elem(text,j)=='l')			//подсчёт сколько букв в в строке "Hello world"
		{
			k = k+1
		}
	}
	print(k)
	
	/**
		цикл while
		~ - элемент, обозначающий конец строки
	**/
	while(elem(str,i)!='~')
	{
		char v = elem(str,i)
		WriteToFile("D:\921701\Yakimchik\test2.txt",v)
		i=i+1
	}
	
	int sum = k+15+kol+func(k)			// работа с int и вызов глобальной функции, возращающей тип int
}